spring:
  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_REPO_URL}
          cloneOnStart: true
          username: ${CONFIG_GIT_USERNAME}
          password: ${CONFIG_GIT_PASSWORD}